name: Validate infrastructure

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
  security-events: write

jobs:
  terraform:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.10.5
      - name: Format and validate Terraform
        run: |
          terraform fmt -check -recursive
          for directory in modules/network modules/eks environments/dev; do
            terraform -chdir="$directory" init -backend=false -input=false
            terraform -chdir="$directory" validate
          done

  manifests:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: azure/setup-kubectl@v4
      - name: Render manifests
        run: kubectl kustomize kubernetes >/dev/null

  security:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: bridgecrewio/checkov-action@v12
        with:
          directory: .
          framework: terraform,kubernetes
          # KMS key policies require the account-root delegation statement.
          # The rationale is documented in SECURITY.md; do not broaden this list.
          skip_check: CKV_AWS_109,CKV_AWS_111,CKV_AWS_356
          quiet: true
